Lessons learned:
If bindException:
- netstat -ao
taskkill /F /PID <pid number>

mvn jetty:run
mvn clean
mvn package

initial import of files
Adjust the pom.xml to your liking. You probably want to change the artifact and group id�s.
Run �mvn clean� to create a clean project with the dependencies in the right location.
Run �mvn eclipse:eclipse� to generate the Eclipse configuration files.
Import the project into Eclipse (or you can use the m2eclipse Maven integration if you like)

git initialization in github: [https://help.github.com/articles/create-a-repo]
git config --global user.name "Your Name Here"
git config --global user.email "your_email@example.com"
email address for Git should be the same one associated with your GitHub account. 
git init
git remote add origin https://github.com/username/<projectName>.git  or
git remote set-url origin git@github.com:user/repo.git
git push origin master

aliasing   - [http://cheat.errtheblog.com/s/git]
	go to cd ~
	vi .gitconfig
	[alias]
		st = status

apache axis has wsdl2java and java2wsdl not in cxf
		
using ssh for github: [https://help.github.com/articles/generating-ssh-keys]
cd ~/.ssh
ssh-keygen -t rsa -C "your_email@example.com"
do passphrase
clip < ~/.ssh/id_rsa.pub
add key from account setttings
ssh -T git@github.com --> to test

/*	private Component generateContent() {
		CustomLayout pageBodyLayout;
		pageBodyLayout = generateLayout();
		pageBodyLayout.addComponent(generateLogin(), "loginLayout");
		return pageBodyLayout;
	}*/
	/*	private CustomLayout generateLayout() {
		String loginLayoutString = "<div class ='loginLayout' style='float:center'>" +
							"<div location ='centerLogin'/>" +
							"</div>";
		CustomLayout loginLayoutPage = null;
		try {
			loginLayoutPage = new CustomLayout(new ByteArrayInputStream(loginLayoutString.getBytes()));
		} catch (IOException e) {
			e.printStackTrace();
		}
		return loginLayoutPage;
	}*/